// A module is somewhat like a namespace
// It should match a URI-ish where the project resides if you want the go toolchain to understand how to work with it remotely
module github.com/jolheiser/myapp

go 1.19
